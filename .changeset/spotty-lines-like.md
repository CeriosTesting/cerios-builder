---
"@cerios/cerios-builder": minor
---

build() now does runtime and compile time validation. So is now the same as buildSafe which is deprecated now. Also added new build methods to have more control.
